#余额变动记录
query TransactionsQuery($first: Int, $page: Int) {
    transactions(first: $first, page: $page) {
    	paginatorInfo {
          	currentPage
          	hasMorePages
        }
    	data {
          	id
            amount
            remark
            created_at
            balance
        }
    }
}